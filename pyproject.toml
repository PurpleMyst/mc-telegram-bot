[project]
name = "mc-telegram-bot"
version = "0.1.0"
description = "Telegram bot to manage MC servers"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "platformdirs>=4.3.6",
    "pretty-errors>=1.2.25",
    "python-dotenv>=1.0.1",
    "python-telegram-bot[all]>=21.9",
]
